let initialState = {
  string: 0,
};

export const stringReducer = (state = initialState, action) => {
  switch (action.type) {
    /*     case "A":
      break;
 */
    default:
      return state;
  }
};
